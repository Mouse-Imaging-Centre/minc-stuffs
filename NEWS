New in Version 0.1.3
====================
* compute_values_across_segmentation now accepts label files with up to 65536 labels (instead of 256)

New in Version 0.1.2
====================
* minc-stuffs can now be compiled against the minc-toolkit
